#include<stdio.h>
int main()
{
    int n,str[n];
    printf("enter the number of characters of an string: ");
    scanf("%d",&n);

    printf("enter the string: ");
    scanf("%s",&str[n]);

    reverce();
    return 0;
}
int reverse()
{
    
}